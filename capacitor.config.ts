import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.psu.walktober', //io.ionic.starter
  appName: 'Walktober',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
